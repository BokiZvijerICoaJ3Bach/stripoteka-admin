import React from 'react'

const Carts = props=>{
    return(
        <div>
        <p>{}props.ID</p>
        <p>{props.ime}</p>
        <p>{props.naslov}</p>
        </div>
    )
}

export default Carts
